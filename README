Описание проекта

Это прототип игрового слота 5×3 с простым UI, реализованный на TypeScript и Pixi.js, с реактивной архитектурой MVVM.
Основная цель проекта — показать чистое разделение доменной логики, состояния и визуальной сцены, а также предсказуемое управление потоком игры.

Особенности:

Рилы 5×3 с плавным вращением и остановкой.

Плавная остановка с GSAP, без резких скачков символов.

Авто-стоп спина через заданное время.

Реактивное управление состоянием через ViewModel.

UI для управления ставкой, спином и автоспином.

🚀 Быстрый старт

1. Клонируем репозиторий:

git clone <your-repo-url>
cd <your-repo-folder>


2. Устанавливаем зависимости:

npm install


3. Запуск в режиме разработки:

npm run dev


4. Открываем в браузере:
По умолчанию Vite запускает сервер на http://localhost:5173
.

🏗 Архитектура

Проект построен по принципу MVVM + отдельная сцена Pixi.js + машина состояний:

1️⃣ Доменная модель (SlotModel)

Содержит только данные и правила их изменения:

баланс, ставка, выигрыш

проверка возможности спина (canSpin)

применение результата спина (applySpin)

Не зависит от Pixi.js, UI, таймеров и анимаций.

2️⃣ ViewModel (SlotViewModel)

Является единственным реактивным контрактом состояния, наблюдаемым UI и сценой.

Отражает данные модели и предоставляет “готовые” для отображения значения (баланс, текущая ставка, выигрыш, текущая фаза спина).

Получает команды от UI (Spin, Stop, Auto-spin) и обновляет модель.

Не содержит ссылок на сцену и не управляет её жизненным циклом.

3️⃣ Сцена (SlotScene)

Реализует рилы и их анимацию через Pixi.js.

Подписывается на ViewModel и реагирует на изменения:

запускает рилы при начале спина

замедляет и останавливает рилы

сообщает ViewModel о завершении спина и выигрыше

Внутри сцены реализована машина состояний, которая управляет этапами:

ожидание

вращение

показ выигрыша

возврат в ожидание

4️⃣ UI (SlotUI)

Отображает кнопки спина, автоспина, выбор ставки, текущий баланс и выигрыш.

Все действия пользователя передаются только через ViewModel.

Не обращается к сцене напрямую.

📊 Поток данных
[Пользователь]
     |
     v
    UI (SlotUI) --(команды)--> ViewModel
     |                              |
     |                              v
     |                          SlotModel
     |                              ^
     v                              |
  SlotScene <---(подписка)--- ViewModel


Пользователь нажимает кнопку Spin в UI.

UI вызывает метод ViewModel (startSpin).

ViewModel проверяет баланс и обновляет фазу спина.

SlotScene наблюдает ViewModel и запускает анимацию рилов.

Машина состояний внутри сцены управляет этапами игры.

После остановки рилов Scene сообщает ViewModel о результате спина.

ViewModel обновляет баланс и состояние модели.

UI автоматически обновляет отображение через реактивную подписку.

⚡ Технологии

TypeScript

Pixi.js

GSAP (плавная остановка символов)

Vite (сборка и dev-сервер)
